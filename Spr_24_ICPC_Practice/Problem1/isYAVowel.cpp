#include <iostream>
#include <string>

using namespace std;

/*
int main() {
    int is = 0, no = 0;

    string str;
    getline(cin, str);

    for (char ch : str) {
        if (ch == 'a' || ch == 'e' || ch == 'o' || ch == 'i' || ch == 'u')
        {
            is++;
            no++;
        }
        else if (ch == 'y')
            is++;
    }

    cout << no << " " << is << endl;
}

*/